name: Docker Build Pipeline

on:
  push:
    branches:
      - 'main'
    paths:
      - 'Dockerfile'
  pull_request:
    branches:
      - 'main'
    paths:
      - 'Dockerfile'

jobs:
  call-workflow:
    uses: kiriti07/pipelines/.github/workflows/docker_build.yml@main
